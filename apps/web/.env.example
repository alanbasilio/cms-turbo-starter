# Base URL of the Strapi backend (without trailing /api), used server-side by the
# BFF proxy and the auth Server Actions. The browser never calls Strapi directly.
STRAPI_API_URL=http://localhost:1337

# Optional public fallback. Kept for convenience; STRAPI_API_URL takes precedence.
NEXT_PUBLIC_API_URL=http://localhost:1337
